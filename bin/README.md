# Binaries Directory

This directory is intended for `ffmpeg.exe` and `ffprobe.exe` executables.

**Note:** If these files are missing, they will be automatically downloaded by the application on the first start.
